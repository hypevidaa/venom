import { GenericAccount } from "everscale-standalone-client";
import { Address } from "everscale-inpage-provider";
export declare class LockliftWallet extends GenericAccount {
    constructor(args: {
        address: string | Address;
        publicKey?: string;
    });
}
